# Root environment reference — copy per-app examples:
#   apps/api/.env.example  → apps/api/.env
#   apps/web/.env.example  → apps/web/.env.local
#   apps/backend/.env.example → apps/backend/.env

NODE_ENV=production

# VPS deployment paths
DEPLOY_DIR=/var/www/fetcherio
DEPLOY_USER=deploy

# Domains
MARKETING_URL=https://fetcherio.dev
APP_URL=https://app.fetcherio.dev
API_URL=https://api.fetcherio.dev
ADMIN_URL=https://admin.fetcherio.dev
DOCS_URL=https://docs.fetcherio.dev
